/**
 * DISABLED — Zero-Demo Data Policy
 *
 * This seeder script is permanently disabled per the Zero-Demo Data Policy.
 * All data must enter the system through genuine customer-facing flows.
 *
 * No fake, sample, or generated data is permitted in any environment.
 * Attempting to run this script will exit immediately with a non-zero status.
 *
 * If you need to set up a new Stripe environment, use real product data only
 * and add it through the Stripe Dashboard directly.
 */

console.error(
  "\n❌ ZERO-DEMO DATA POLICY VIOLATION\n" +
  "   This seeder is permanently disabled.\n" +
  "   Add real products via the Stripe Dashboard.\n"
);
process.exit(1);
